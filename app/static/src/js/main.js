$(function () {
	/*
	 * Template:
	 *
     * $('html.section_name').each(function () {
	 * 	// call initialization functions for the section
	 * 	bindSectionName();
	 * });
	 *
	 */
    $('html.section_name').each(function () {
        bindSectionName();
    });
});
